#Postgres DB
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
#specific variable without which Docker doesn't save the data in volumes:
PGDATA=/var/lib/postgresql/data/pgdata

#pgadmin4 details
PGADMIN_DEFAULT_EMAIL:
PGADMIN_DEFAULT_PASSWORD: